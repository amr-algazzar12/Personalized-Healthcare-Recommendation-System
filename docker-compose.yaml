version: "3"

# =============================================================================
# docker-compose.yaml
# Self-contained cluster for the Personalized Healthcare Recommendation System.
# Derived from mrugankray/Big-Data-Cluster — trimmed to required services only.
# All credentials match the original cluster exactly.
# =============================================================================

services:

  # ---------------------------------------------------------------------------
  # NameNode: HDFS + Spark + Airflow + MiniConda — all in one container
  # ---------------------------------------------------------------------------
  namenode:
    image: mrugankray/namenode-spark-airflow-flume-zepplin:1.1
    container_name: namenode
    restart: always
    ports:
      - "9870:9870"   # HDFS NameNode UI
      - "9000:9000"   # HDFS RPC
      - "8080:8080"   # Spark Master UI
      - "8081:8081"   # Spark Worker UI
      - "4040:4040"   # Spark Driver UI (active during jobs)
      - "3000:3000"   # Airflow UI
    volumes:
      - hadoop_namenode:/hadoop/dfs/name
      - hadoop_namenode_conda:/root/anaconda
      - hadoop_namenode_spark:/opt/spark
      - airflow_namenode:/root/airflow
      - ./configs/namenode_bashrc.txt:/root/.bashrc
      - ./configs/namenode_airflow.cfg:/root/airflow/airflow.cfg
      - ./dags:/root/airflow/dags
      - ./src:/home/src
      - ./data/raw:/home/data/raw
      - ./models:/home/models
      - ./conda:/home/conda
      - ./scripts:/home/scripts
      - mlflow_artifacts:/home/mlflow_artifacts
    environment:
      - CLUSTER_NAME=hadoop-learning
      - CORE_CONF_fs_defaultFS=hdfs://namenode:9000
      - HDFS_CONF_dfs_replication=1
      - HDFS_CONF_dfs_namenode_replication_min=1
      - PYSPARK_PYTHON=/root/anaconda/envs/healthcare-rec/bin/python
      - PYSPARK_DRIVER_PYTHON=/root/anaconda/envs/healthcare-rec/bin/python
      - MLFLOW_TRACKING_URI=http://mlflow:5001
    env_file:
      - ./hadoop.env

  # ---------------------------------------------------------------------------
  # DataNode
  # ---------------------------------------------------------------------------
  datanode:
    image: mrugankray/datanode-python:1.0
    container_name: datanode
    restart: always
    volumes:
      - hadoop_datanode:/hadoop/dfs/data
      - hadoop_datanode_conda:/root/anaconda
      - ./configs/datanode_bashrc.txt:/root/.bashrc
    environment:
      SERVICE_PRECONDITION: "namenode:9870"
      CORE_CONF_fs_defaultFS: "hdfs://namenode:9000"
      HDFS_CONF_dfs_replication: "1"
      HDFS_CONF_dfs_datanode_use_datanode_hostname: "false"
    ports:
      - "9864:9864"
    env_file:
      - ./hadoop.env

  # ---------------------------------------------------------------------------
  # YARN ResourceManager
  # ---------------------------------------------------------------------------
  resourcemanager:
    image: mrugankray/resourcemanager-python:1.0
    container_name: resourcemanager
    restart: always
    volumes:
      - hadoop_resourcemanager_conda:/root/anaconda
      - ./configs/resourcemanager_bashrc.txt:/root/.bashrc
    environment:
      SERVICE_PRECONDITION: "namenode:9000 namenode:9870 datanode:9864"
    ports:
      - "8088:8088"
    env_file:
      - ./hadoop.env

  # ---------------------------------------------------------------------------
  # YARN NodeManager
  # ---------------------------------------------------------------------------
  nodemanager:
    image: mrugankray/nodemanager-python:1.0
    container_name: nodemanager
    restart: always
    volumes:
      - hadoop_nodemanager_conda:/root/anaconda
      - ./configs/nodemanager_bashrc.txt:/root/.bashrc
    environment:
      SERVICE_PRECONDITION: "namenode:9000 namenode:9870 datanode:9864 resourcemanager:8088"
    ports:
      - "8042:8042"
      - "19888:19888"
    env_file:
      - ./hadoop.env

  # ---------------------------------------------------------------------------
  # History Server
  # ---------------------------------------------------------------------------
  historyserver:
    image: bde2020/hadoop-historyserver:2.0.0-hadoop3.2.1-java8
    container_name: historyserver
    restart: always
    environment:
      SERVICE_PRECONDITION: "namenode:9000 namenode:9870 datanode:9864 resourcemanager:8088"
    ports:
      - "8188:8188"
    volumes:
      - hadoop_historyserver:/hadoop/yarn/timeline
    env_file:
      - ./hadoop.env

  # ---------------------------------------------------------------------------
  # Hive Server — Hive 2.3.2 + SSH (for Airflow SSHOperator)
  # All four config files MUST be mounted or the container exits immediately.
  # ---------------------------------------------------------------------------
  hive-server:
    image: mrugankray/hive-server-sqoop:1.0
    container_name: hive-server
    restart: always
    depends_on:
      - namenode
      - datanode
      - hive-metastore
    volumes:
      - hadoop_hive_server_sqoop:/usr/lib/sqoop
      - ./configs/hive_server/sqoop-env.sh:/usr/lib/sqoop/conf/sqoop-env.sh
      - ./configs/hive_server/hive_server_bashrc.txt:/root/.bashrc
      - ./configs/hive_server/sqoop-site.xml:/usr/lib/sqoop/conf/sqoop-site.xml
      - ./configs/hive_server/sshd_config.conf:/etc/ssh/sshd_config
      - ./src:/home/src
    environment:
      HIVE_CORE_CONF_javax_jdo_option_ConnectionURL: "jdbc:postgresql://hive-metastore/metastore"
      SERVICE_PRECONDITION: "hive-metastore:9083"
    ports:
      - "10000:10000"
    env_file:
      - ./hadoop.env

  # ---------------------------------------------------------------------------
  # Hive Metastore
  # NOTE: On first boot run: make init-hive-schema
  # ---------------------------------------------------------------------------
  hive-metastore:
    image:  bde2020/hive:2.3.2-postgresql-metastore
    container_name: hive-metastore
    restart: always
    depends_on:
      hive-metastore-postgresql:
        condition: service_healthy
    command: /opt/hive/bin/hive --service metastore
    environment:
      SERVICE_PRECONDITION: "namenode:9000 namenode:9870 datanode:9864 hive-metastore-postgresql:5432"
      HIVE_OPTS: "--hiveconf hive.metastore.schema.verification=false"
    ports:
      - "9083:9083"
    env_file:
      - ./hadoop.env

  # ---------------------------------------------------------------------------
  # Hive Metastore PostgreSQL — INTERNAL ONLY, never use from app code
  # ---------------------------------------------------------------------------
  hive-metastore-postgresql:
    image: bde2020/hive-metastore-postgresql:2.3.0
    container_name: hive-metastore-postgresql
    restart: always
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U hive -d metastore"]
      interval: 10s
      timeout: 5s
      retries: 5

  # ---------------------------------------------------------------------------
  # External PostgreSQL — Airflow DB + MLflow backend + app
  # Credentials: user=external  password=external  db=external
  # ---------------------------------------------------------------------------
  external_postgres_db:
    image: postgres:15.1-alpine3.17
    container_name: external_postgres_db
    restart: always
    volumes:
      - external_postgres_db_data:/var/lib/postgresql/data/
      - ./configs/postgres_init.sql:/docker-entrypoint-initdb.d/init.sql
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: external
      POSTGRES_PASSWORD: external
      POSTGRES_DB: external

  # ---------------------------------------------------------------------------
  # pgAdmin 4
  # ---------------------------------------------------------------------------
  external_pgadmin:
    image: dpage/pgadmin4:6.18
    container_name: external_pgadmin
    restart: always
    ports:
      - "5000:80"
    environment:
      - PGADMIN_DEFAULT_EMAIL=pgadmin@xyz.com
      - PGADMIN_DEFAULT_PASSWORD=external
    depends_on:
      - external_postgres_db

  # ---------------------------------------------------------------------------
  # MLflow tracking server
  # ---------------------------------------------------------------------------
  mlflow:
    build:
      context: ./docker/mlflow
      dockerfile: Dockerfile
    container_name: mlflow
    restart: always
    ports:
      - "5001:5001"
    volumes:
      - mlflow_artifacts:/home/mlflow_artifacts
    environment:
      MLFLOW_BACKEND_STORE_URI: postgresql://external:external@external_postgres_db:5432/mlflow
      MLFLOW_ARTIFACT_ROOT: /home/mlflow_artifacts
    depends_on:
      - external_postgres_db
    command: >
      mlflow server
      --backend-store-uri postgresql://external:external@external_postgres_db:5432/mlflow
      --default-artifact-root /home/mlflow_artifacts
      --host 0.0.0.0
      --port 5001

  # ---------------------------------------------------------------------------
  # Flask API + Streamlit dashboard
  # ---------------------------------------------------------------------------
  app:
    build:
      context: ./docker/app
      dockerfile: Dockerfile
    container_name: app
    restart: always
    ports:
      - "5050:5050"
      - "8501:8501"
    volumes:
      - ./src:/home/src
      - ./models:/home/models
      - mlflow_artifacts:/home/mlflow_artifacts
    environment:
      MLFLOW_TRACKING_URI: http://mlflow:5001
      FLASK_ENV: development
      FLASK_PORT: "5050"
      PYTHONPATH: /home/src
    depends_on:
      - mlflow
    command: >
      bash -c "python /home/src/api/app.py & streamlit run /home/src/dashboard/app.py --server.port 8501 --server.address 0.0.0.0"

# =============================================================================
# Volumes
# =============================================================================
volumes:
  hadoop_namenode:
  hadoop_namenode_conda:
  hadoop_namenode_spark:
  hadoop_datanode:
  hadoop_datanode_conda:
  hadoop_resourcemanager_conda:
  hadoop_nodemanager_conda:
  hadoop_historyserver:
  hadoop_hive_server_sqoop:
  external_postgres_db_data:
  airflow_namenode:
  mlflow_artifacts: